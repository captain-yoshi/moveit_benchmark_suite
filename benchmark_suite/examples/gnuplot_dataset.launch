<?xml version="1.0" ?>
<launch>

  <!-- By default, we are not in debug mode -->
  <arg name="debug" default="false" />
  <arg unless="$(arg debug)" name="launch_prefix" value="" />
  <arg     if="$(arg debug)" name="launch_prefix" value="gdb --args" />

  <node name="gnuplot_dataset" pkg="moveit_benchmark_suite" type="gnuplot_dataset" respawn="false" required="true" output="screen" launch-prefix="$(arg launch_prefix)">

    <rosparam>
      files:
        - /home/captain-yoshi/.ros/db/collision_check1.yaml
        - /home/captain-yoshi/.ros/db/collision_check2.yaml
    </rosparam>

  </node>
</launch>
